import { css } from 'styled-components';

const Card = css`
  background-color: var(--clr-white-1);
  box-shadow: 0 0 0.5em 0.25em var(--clr-transparent-1);
  padding: 1em;
  border-radius: 5px;
`;

export default Card;
